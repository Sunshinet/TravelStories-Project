/* globals $ */

$(document).ready(function($) {
    $('.capital').textEffect({
        effect: 'fade',
        effectSpeed: 35,
    });
    $('.italic').textEffect({
        effect: 'fade',
        effectSpeed: 35,
    });
});
