extends ../main
block content
    each story in context
        div.singleStory
          h1 
            =story.place.name
          h1
            a(href='/stories/id=' + story._id)
              =story.titleStory
          div
            h2
              =story.user.username

