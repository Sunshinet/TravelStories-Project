extends main
block content
    div
        h1
            | All Places
        each place in context
            div
                h2
                   a(href='place/' + place.name) 
                        =place.name